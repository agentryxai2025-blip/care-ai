kind = "web"
previewPath = "/"
title = "Agentryx — Care Platform"
version = "1.0.0"
id = "artifacts/agentryx"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19691

[services.development]
run = "pnpm --filter @workspace/agentryx run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/agentryx", "run", "build" ]
publicDir = "artifacts/agentryx/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19691"
BASE_PATH = "/"
